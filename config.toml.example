# Velo Rift Project Configuration
# Copy this to .vrift/config.toml in your project

[ingest]
# Patterns to ignore during live ingest and file watching
# Add more patterns as needed for your project
ignore_patterns = [
    ".git",
    # Add project-specific ignores below:
    # "node_modules",
    # "target",
    # "__pycache__",
]
