# Test Suite Configuration (Rust-only for now)

# Python Test Paths (Empty as we don't have python tests yet)
TEST_PATHS_QUICK="tests/qa/quick"
TEST_PATHS_FULL="tests/qa"
TEST_PATHS_DOCKER="tests/qa"

TIER0_TESTS=()
TIER1_TESTS=()
TIER2_TESTS=()
TIER3_TESTS=()
